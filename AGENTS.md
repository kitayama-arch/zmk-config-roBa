# このプロジェクトの作業ルール

- コミットメッセージは日本語で書く。

## フォーク元への非干渉

- 作業対象はユーザー所有の `kitayama-arch/zmk-config-roBa` とそのローカルチェックアウト。
- フォーク元の `kumamuk-git/zmk-config-roBa` および他者所有の依存リポジトリは読み取り専用として扱う。
- 上流への push、PR・Issue・コメントの作成、レビュー依頼、Actions の起動、設定変更など、上流の状態変更や通知を発生させる操作を行わない。
- 上流のコード・履歴の閲覧、clone、fetch、差分取得は許可する。取り込み・競合解消・検証はユーザー側だけで行う。
- push・PR・Actions 実行時は対象の owner/repository を明示して確認する。フォーク元に向き得る GitHub CLI の既定値や `gh pr create` の自動選択に依存しない。PR が必要な場合も base/head ともユーザー所有リポジトリ内に限定する。
- upstream remote を追加する場合は、取得用 URL と別に push URL を無効なローカルパスに設定し、誤 push を防ぐ。これは補助対策であり、GitHub API や CLI による上流への書き込みも禁止する。

## 上流変更の取り込み

- 現在の状態を保存し、`codex/` 接頭辞の作業ブランチで差分を取り込む。未コミット・未追跡ファイルを削除または上書きしない。
- 上流の対象ブランチとコミット SHA を確認・記録し、必要な変更と依存関係を一緒に確認する。上流のキーマップでユーザーのキーマップを丸ごと置き換えない。
- キー配置、独自レイヤー、ロータリーエンコーダ、JIS 配列補正、トラックボールの向き・スクロール・オートマウス設定を保持する。上流とレイヤー番号が異なる場合はユーザー側の番号へ移植する。
- ビルド成功と実機動作確認は分けて報告する。実機へのファームウェア書き込みは、準備完了を報告した後のユーザーによる明示許可を受けて行う。
